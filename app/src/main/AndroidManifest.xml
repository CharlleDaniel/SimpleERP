<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.simpleerp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/simple"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuPrincipal"
            android:label=""
            android:theme="@style/AppTheme2" >
        </activity>
        <activity
            android:name=".ListFilesActivity"
            android:label="@string/title_activity_drive"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme2" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.simpleerp.MainActivity" />
        </activity>
        <activity
            android:name=".CriarArquivo"
            android:label="@string/title_activity_criar_arquivo" >
        </activity>
    </application>

</manifest>
